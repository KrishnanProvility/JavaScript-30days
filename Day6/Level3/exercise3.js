
const webTechs = [
    'HTML',
    'CSS',
    'JavaScript',
    'React',
    'Redux',
    'Node',
    'MongoDB'
  ]
 
 
 const mernStack = ['MongoDB', 'Express', 'React', 'Node']
 
 
  let sortedWebTechs =[];
 
  for(let element of webTechs)
  {
    sortedWebTechs.push(element);
  }
  
  console.log(sortedWebTechs.sort());
 
 
  let sortedmernstack =[];
 
  for(let element of mernStack)
    {
      sortedmernstack.push(element);
    }
    
    console.log(sortedmernstack.sort());
   
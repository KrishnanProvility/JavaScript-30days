 //excercise 12:
 console.log("excercise 12:");
 let getSubStr ="You cannot end a sentence with because because because is a conjunction";
 let startIndex =getSubStr.indexOf("because",1)
 let lastIndexs =getSubStr.lastIndexOf("e")
console.log(getSubStr.substring(startIndex,lastIndexs+1))//because because because
